--- a/fraud-detection/main.py
+++ b/fraud-detection/main.py
@@ -9,26 +9,14 @@
 
 from pythonjsonlogger import jsonlogger
 
-# ── DATADOG APM ───────────────────────────────────────────────────────
-# Uncomment to enable auto-instrumentation. Must be the FIRST import
-# before any other library is imported.
-# Requires: ddtrace installed, DD_API_KEY / DD_ENV / DD_SERVICE / DD_VERSION set.
-# Docs: https://docs.datadoghq.com/tracing/trace_collection/dd_libraries/python/
 #
-# from ddtrace import patch_all
-# from ddtrace.contrib.logging import patch as patch_logging
-# patch_all()
-# patch_logging()
-# ─────────────────────────────────────────────────────────────────────
+from ddtrace import patch_all
+from ddtrace.contrib.logging import patch as patch_logging
+patch_all()
+patch_logging()
 
-# ── DATADOG CONTINUOUS PROFILER ───────────────────────────────────────
-# Uncomment to start the Continuous Profiler. It runs as a background
-# thread and captures CPU / wall-time / memory profiles every 60 s.
-# Correlates with APM traces to identify which span is burning CPU.
-# Docs: https://docs.datadoghq.com/profiler/enabling/python/
 #
-# import ddtrace.profiling.auto  # noqa: F401  — side-effect import, keep at top
-# ─────────────────────────────────────────────────────────────────────
+import ddtrace.profiling.auto  # noqa: F401  — side-effect import, keep at top
 
 import stomp
 
--- a/fraud-detection/requirements.txt
+++ b/fraud-detection/requirements.txt
@@ -9,7 +9,7 @@
 # Progression steps in README.md.
 #
 # Step 3-7  — APM tracing, log injection, profiler, custom spans, DSM
-# ddtrace[data_streams]==2.9.0
+ddtrace[data_streams]==2.9.0
 #
 # Step 6    — DogStatsD custom metrics (finance.fraud.score gauge)
 # datadog==0.49.1
--- a/fraud-detection/listener.py
+++ b/fraud-detection/listener.py
@@ -19,7 +19,7 @@
 # Requires: ddtrace installed + patch_all() called in main.py first.
 # Docs: https://docs.datadoghq.com/tracing/trace_collection/custom_instrumentation/python/
 #
-# from ddtrace import tracer
+from ddtrace import tracer
 # ─────────────────────────────────────────────────────────────────────
 
 # ── DATADOG DATA STREAMS MONITORING ──────────────────────────────────
@@ -129,22 +129,19 @@
         # Use correlation_id only if your broker reuses a bounded set.
         # Docs: https://docs.datadoghq.com/tracing/trace_collection/custom_instrumentation/python/
         #
-        # with tracer.trace("fraud.score", service="fraud-detection", resource=transaction_type) as span:
-        #     result = score({"transaction_id": transaction_id, "amount": amount})
-        #     span.set_tag("transaction.type", transaction_type)
-        #     span.set_tag("payment.currency", currency)
-        #     span.set_tag("fraud.score_bucket", result["bucket"])
-        #     span.set_tag("messaging.destination", "fraud.score.queue")
-        #     # HIGH-CARDINALITY WARNING: messaging.message_id is per-message —
-        #     # tag only when debugging a specific incident, not in production.
-        #     # span.set_tag("messaging.message_id", message_id)
-        #     if result["bucket"] == "high":
-        #         span.error = 1
-        #         span.set_tag("error.message", "High-risk transaction flagged")
-        # ─────────────────────────────────────────────────────────────
+        with tracer.trace("fraud.score", service="fraud-detection", resource=transaction_type) as span:
+            result = score({"transaction_id": transaction_id, "amount": amount})
+            span.set_tag("transaction.type", transaction_type)
+            span.set_tag("payment.currency", currency)
+            span.set_tag("fraud.score_bucket", result["bucket"])
+            span.set_tag("messaging.destination", "fraud.score.queue")
+            # HIGH-CARDINALITY WARNING: messaging.message_id is per-message —
+            # tag only when debugging a specific incident, not in production.
+            # span.set_tag("messaging.message_id", message_id)
+            if result["bucket"] == "high":
+                span.error = 1
+                span.set_tag("error.message", "High-risk transaction flagged")
 
-        result = score({"transaction_id": transaction_id, "amount": amount})
-
         # ── DATADOG DOGSTATSD — GAUGE ─────────────────────────────────
         # Emit a gauge for each scored message. The value is the raw
         # score float (useful for aggregation in Datadog metrics), but
